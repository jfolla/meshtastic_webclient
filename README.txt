README moved to README.md

This package includes optional HTTPS/self-signed support via:

  python app.py --host 192.168.0.18 --listen-host 0.0.0.0 --listen-port 8088 --ssl-adhoc

Install PyOpenSSL in the virtual environment first:

  python -m pip install pyopenssl
